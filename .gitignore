/result
/target
# cargo-fuzz crate (ts_tka/fuzz is its own workspace) — its nested build dir + generated corpus/crashes
ts_tka/fuzz/target/
ts_tka/fuzz/corpus/
ts_tka/fuzz/artifacts/
# produced by cargo-bisect-rustc
target-bisector-*/
.idea/
flamegraph.svg
perf.data
perf.data.old
heaptrack.*
tsrs_keys*.json
priv/
_build/
ts_elixir/deps/
ts_python/python/tailscale/*.so
*.exe
*.dll
*.lib
tailscale.h

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
.omc/

# Cross-lane working docs — intentionally untracked (never commit; they carry working state)
docs/COORDINATE.md
docs/GOAL.md

# Local research working docs — deviation-hunt notes, never committed
docs/research/
docs/AUTOPILOT-WORKLOG.md
docs/DEFERRED-QUESTIONS.md

# mipsel cross-build CARGO_HOME cache (scripts/build-mipsel.sh)
.cargo-home-mipsel/
