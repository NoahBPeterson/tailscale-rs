[package]
name = "geiserx_ts_metrics"
version.workspace = true
description = "in-process client metrics registry (clientmetric parity) with Prometheus text export"
categories = ["network-programming"]
keywords = ["tailscale", "metrics", "clientmetric", "prometheus"]

repository.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true


[lib]
name = "ts_metrics"
[dependencies]
# mipsel and other 32-bit targets lack native AtomicU64; portable-atomic provides it.
portable-atomic.workspace = true

[lints]
workspace = true
